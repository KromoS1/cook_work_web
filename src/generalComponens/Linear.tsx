import React,{FC, memo} from 'react'
import {LinearProgress} from "@mui/material";

export const Linear:FC = memo(() => {

return (
        <div>
            <LinearProgress color="inherit" />
        </div>
    )
})
